# Guardianity hackathon demo — dependencies.
# NOTE: the heart (conscience) is NOT here — it runs on our hosted service. So this
# list is light: a web server, an HTTP client to the heart, and your local model.
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7
httpx>=0.27
# the local model (your vessel). torch is installed CPU-only by run.sh; on a GPU
# (e.g. an A100) install the CUDA build of torch yourself, then set HACK_DEVICE=cuda.
# transformers>=4.53 so newer architectures (e.g. Gemma 3n / "E4B") load correctly;
# timm is Gemma 3n's vision-tower dependency (required even for text use).
transformers>=4.53
huggingface-hub>=0.26
accelerate>=0.30
timm>=1.0
fpdf2>=2.7          # research -> PDF document writer
ddgs>=9.0          # web search for auto-research
